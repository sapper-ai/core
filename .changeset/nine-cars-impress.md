---
'sapper-ai': patch
---

Remove the optional `@sapper-ai/openai` peer dependency and simplify the default web/demo surface by dropping OpenAI Agents-specific quickstart and playground paths. Also remove the legacy `sapper-ai dashboard` command path as part of monorepo cleanup.
